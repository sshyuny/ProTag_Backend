spring:
  application:
    name: protag
  jpa:
    show-sql: false
    generate-ddl: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    mysql:
      jdbc-url: jdbc:mysql://localhost:3306/project_db
      username: pjadminuser
      password: pw11
      driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
